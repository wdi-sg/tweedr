var React = require("react");
var Default = require("../default");

class Login extends React.Component {
  render() {
    return (
      <Default>
        <p>Wah bludder, u nvr login how u wan tweed?</p>
      </Default>
    );
  }
}

module.exports = Login;
